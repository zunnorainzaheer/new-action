name: 'Hello World'
description: 'Greet someone'

runs:
  using: "composite"
  steps:
    - uses: zunnorainzaheer/new-action@main
    - name: Golang
      uses: actions/setup-go@v2
      with:
        go-version: '1.16.8'
    - shell: bash
      run: go run main.go